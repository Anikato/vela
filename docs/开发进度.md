# Vela — 开发进度

> 本文档记录项目的实时开发状态，每完成一个模块后更新。  
> 开发阶段定义见 [技术方案 § 十五、开发顺序](技术方案.md)。

---

## 当前阶段：P13（P12 已全部完成）

---

## 各阶段完成情况

### P0 — 基础架构

| 任务 | 状态 | 备注 |
|------|------|------|
| 项目初始化（Next.js 15 + TypeScript + Tailwind CSS 4） | ✅ 完成 | |
| 安装核心依赖（Drizzle、Zod、Auth.js、Sharp、Nodemailer 等） | ✅ 完成 | |
| 基础配置（drizzle.config、docker-compose、.env.example） | ✅ 完成 | 已补全 Dockerfile（standalone 多阶段构建）+ docker-compose app 服务 |
| 全部数据库 Schema（14 模块 34 张表） | ✅ 完成 | |
| 基础工具库（utils、i18n、constants、errors、types） | ✅ 完成 | |
| 语言管理模块（Service + Action + 后台 UI） | ✅ 完成 | 第一个完整端到端模块 |
| 语言标准库统一（中文名称 + Azure/Google 代码映射） | ✅ 完成 | 新增预设语言库，语言管理改为标准化录入 |
| shadcn/ui 初始化 + 基础组件安装 | ✅ 完成 | Button、Table、Dialog、Switch 等 |
| 数据库推送 + 种子数据 | ✅ 完成 | db:push + db:seed 脚本 |

### P1 — 后台布局 + 用户认证 + 文件管理

| 任务 | 状态 | 备注 |
|------|------|------|
| 后台布局（侧边栏导航 + 顶栏 + 响应式，含完整菜单结构） | ✅ 完成 | 5 组导航：内容/营销/外观/多语言/系统 |
| 用户认证（Auth.js + 凭证登录） | ✅ 完成 | Credentials Provider + JWT Session + 路由保护 |
| 用户管理后台（Admin 单角色 CRUD） | ✅ 完成 | `/admin/users`：列表、新建、启停用、重置密码 |
| 文件上传服务（本地存储适配器） | ✅ 完成 | 新增 StorageAdapter + /api/upload + media service（含图片尺寸变体） |
| 媒体管理（后台 UI） | ✅ 完成 | `/admin/media`：上传、列表、复制链接、删除 |
| 富文本编辑器集成 | ✅ 完成 | 新增通用组件 `components/admin/common/rich-text-editor.tsx` |

### P2 — 产品内容管理

| 任务 | 状态 | 备注 |
|------|------|------|
| 分类管理（Service + Action + 后台 UI，含树形拖拽排序） | ✅ 完成 | 已交付 CRUD + 树形拖拽排序（同级重排 + 跨级挂载） |
| 标签管理（Service + Action + 后台 UI） | ✅ 完成 | `/admin/tags`：列表、新建、编辑、删除，多语言名称录入 |
| 产品管理（Service + Action + 后台 UI，含状态生命周期） | ✅ 完成 | `/admin/products`：列表、新建、编辑、删除，含草稿/发布/归档状态 |
| 产品参数管理（分组 + 多语言 + 拖拽排序） | ✅ 完成 | `/admin/products/attributes`：分组/参数 CRUD、拖拽排序、跨组移动 |

### P3 — 页面与区块系统

| 任务 | 状态 | 备注 |
|------|------|------|
| 页面管理（Service + Action + 后台 UI） | ✅ 完成 | `/admin/pages`：列表、新建、编辑、删除，支持首页标记、模板与多语言 SEO |
| 区块渲染器（SectionRenderer + SectionWrapper） | ✅ 完成 | 已落地注册表 + Wrapper 通用配置 + 前台渲染链路（根首页接入） |
| 区块编辑器（后台 UI，含子项管理 + 拖拽排序） | ✅ 完成 | 已交付 MVP：`/admin/pages/[id]/sections` 支持区块 CRUD + 排序；子项管理待后续补齐 |
| 初期区块开发（Hero、富文本、CTA、产品展示等） | ✅ 完成 | 已实现 Hero / Rich Text / CTA / Product Showcase 四个首批区块 |
| 补充区块（特性网格、轮播横幅、数据统计、FAQ、双栏布局） | ✅ 完成 | 新增 feature_grid / carousel_banner / stats / faq / two_column 五种区块 |
| B2B 核心区块扩展（9 种新增 + Hero 增强） | ✅ 完成 | 新增 partner_logos / testimonials / category_nav / video_embed / timeline / image_gallery / team / contact_form / custom_html + hero 背景图支持，区块总数达 18 种 |

### P4 — 导航 + 网站前端布局

| 任务 | 状态 | 备注 |
|------|------|------|
| 导航管理（Service + Action + 后台 UI，4 种链接类型） | ✅ 完成 | `/admin/navigation`：支持 internal/external/category/page，多语言名称与父子层级 |
| 多语言路由中间件（语言检测、重定向、Cookie 记忆） | ✅ 完成 | 已支持默认语言无前缀、非默认语言前缀、默认语言前缀 308 去重定向、Cookie 记忆 |
| 网站 Header（Logo + 导航 + 语言切换器 + 搜索对话框 + 询盘篮图标 + 移动端汉堡菜单） | ✅ 完成 | 动态导航 + 语言切换 + 移动端菜单 + 搜索下拉 + 询盘篮图标（含数量徽标） |
| 网站 Footer（公司信息 + 社交媒体 + 快速导航 + WhatsApp 按钮 + 版权） | ✅ 完成 | 三栏布局（品牌+快速链接+联系方式），社媒 SVG 图标，版权栏，WhatsApp 浮动按钮 |
| Cookie 同意弹窗（多语言文案 + 记住选择） | ✅ 完成 | 前台已接入同意弹窗，文案读取 `ui_translations`（`cookie.*`），选择写入 Cookie 记忆 |
| 面包屑导航通用组件 | ✅ 完成 | 已提供可复用组件 `components/website/layout/breadcrumb.tsx`，待产品/新闻页面接入 |

### P5 — 产品前端页面

| 任务 | 状态 | 备注 |
|------|------|------|
| 产品总览页（/products，含分类树、标签筛选、排序、分页） | ✅ 完成 | 左侧分类树（可折叠多级 + 产品数 + 高亮当前）、标签筛选栏、排序（最新/热门/名称）、SQL 分页 |
| 分类页（/products/[categorySlug]，含子分类 + 附加分类产品） | ✅ 完成 | 分类过滤（含附加分类产品）、子分类通过侧边栏树导航、标签筛选 + 排序同步支持 |
| 产品详情页（/products/[categorySlug]/[productSlug]，含图片画廊、参数表、关联推荐） | ✅ 完成 | 图片画廊（缩略图切换+放大灯箱）、询盘按钮（加入篮+快速询盘）、外贸商业字段、视频嵌入、附件下载、参数分组、关联推荐 |
| 搜索页（/search） | ✅ 完成 | 产品名称（所有语言）+ SKU 搜索，分页，默认/多语言路径，UI 翻译 |
| 联系我们页面（/contact，联系表单 + Google Maps + 联系信息） | ✅ 完成 | 联系表单已接入询盘系统（含 hCaptcha）+ WhatsApp 联系 + Google Maps iframe 嵌入 + 联系信息卡片 |

### P6 — 询盘系统

| 任务 | 状态 | 备注 |
|------|------|------|
| 询盘篮（LocalStorage + 全局状态 + 已归档产品处理） | ✅ 完成 | useSyncExternalStore + LocalStorage，Header 图标带数量徽标，抽屉含数量调整 |
| 询盘表单弹窗（预设字段 + 自定义字段动态渲染） | ✅ 完成 | 预设字段 + 后台自定义字段前台动态渲染（text/number/select/multiselect/file），产品摘要展示 |
| hCaptcha 反垃圾集成 | ✅ 完成 | `@hcaptcha/react-hcaptcha` invisible 模式 + 服务端 API 验证 + 后台 Key 配置 |
| 询盘 Service + Action（提交 + 状态管理） | ✅ 完成 | 创建/列表/详情/状态变更/备注/批量操作/统计，自动生成 INQ-日期-随机码 |
| 询盘邮件通知（通知企业 + 确认客户，双向） | ✅ 完成 | `email.service.ts` Nodemailer 集成，询盘提交异步双向邮件，后台测试邮件连通 |
| 询盘表单字段配置（后台 UI，自定义字段管理） | ✅ 完成 | Service + Action + 后台 CRUD + 拖拽排序 + 多语言标签/占位符/帮助文本 |
| 询盘管理后台（列表筛选 + 详情 + 批量操作 + 内部备注） | ✅ 完成 | 状态卡片统计、搜索筛选、全选批量操作、详情弹窗含产品快照和备注编辑 |
| Header 搜索入口 + 询盘篮图标 | ✅ 完成 | 搜索下拉框 + 询盘篮图标带数量，产品卡片增加"加入询盘"按钮 |

### P7 — 新闻系统

| 任务 | 状态 | 备注 |
|------|------|------|
| 新闻管理（Service + Action + 后台 UI） | ✅ 完成 | CRUD + 多语言翻译 + 富文本编辑 + 封面图 + 草稿/发布状态 |
| 新闻列表页（/news） | ✅ 完成 | 已发布新闻网格展示（封面图+标题+日期+摘要），分页，默认/多语言路径 |
| 新闻详情页（/news/[slug]） | ✅ 完成 | 封面图 + 富文本正文 + 面包屑导航，默认/多语言路径 |

### P8 — SEO

| 任务 | 状态 | 备注 |
|------|------|------|
| 各页面 generateMetadata（title、description、canonical、OG） | ✅ 完成 | 14 个前台页面全部覆盖，含 hreflang 多语言互引、canonical、OG/Twitter Card |
| hreflang 标签（所有语言版本互相引用） | ✅ 完成 | 在 `buildSeoMetadata` 中统一生成 `alternates.languages` + `x-default` |
| 结构化数据 JSON-LD（Product、Organization、BreadcrumbList、Article、FAQPage） | ✅ 完成 | 首页 Organization+WebSite；产品详情 Product+BreadcrumbList；新闻详情 Article+BreadcrumbList |
| 动态 Sitemap（含多语言 alternate + 图片 Sitemap） | ✅ 完成 | `app/sitemap.ts` 动态生成，含静态页+所有分类/产品/新闻，每条 URL 带所有语言 alternate |
| robots.txt | ✅ 完成 | `app/robots.ts` 禁止 /admin/ /api/ /search，指向 sitemap.xml |

### P9 — 主题系统 + UI 翻译

| 任务 | 状态 | 备注 |
|------|------|------|
| 主题系统（CSS 变量 + 主题切换 + 预设主题） | ✅ 完成 | Service + Action + 预设主题自动创建 + 激活/复制/删除 |
| 主题管理后台（颜色、字体、按钮、导航、布局配置） | ✅ 完成 | Tabs 分区编辑（颜色/字体/按钮/导航/布局），HSL 颜色值输入，预设选项组 |
| 主题前端渲染（CSS 变量注入） | ✅ 完成 | `ThemeStyle` Server Component 读取激活主题并注入 `:root` CSS 变量 |
| UI 翻译管理后台（键值对 CRUD + 分类浏览 + 缺失高亮） | ✅ 完成 | 分类侧栏（含完成度百分比）+ 搜索/仅缺失过滤 + 行内编辑 + 新建/删除/分页 |

### P10 — 系统设置 + 高级功能

| 任务 | 状态 | 备注 |
|------|------|------|
| 系统设置后台（站点信息、联系方式、社交媒体、多语言字段） | ✅ 完成 | Tabs 分区：品牌Logo/联系方式/社交媒体/公司信息/多语言字段，媒体选择器 |
| SMTP 邮件配置（后台 UI + 测试邮件功能） | ✅ 完成 | SMTP 服务器/发件人/通知邮箱配置，测试邮件已连通 Nodemailer |
| 脚本注入配置（GA/GTM/Pixel/自定义脚本后台 UI） | ✅ 完成 | GA/GTM/Pixel ID 输入 + 自定义 Head/Body 脚本 |
| 第三方脚本前端渲染（将 GA/GTM/Pixel 实际注入网页） | ✅ 完成 | `AnalyticsScripts` Server Component 读取设置并通过 `next/script` 注入前台 |
| 自动翻译（Azure Translator 集成，单条/批量） | ✅ 完成 | `translation.service.ts` 封装 Azure API + 批量/单条 Action + UI 翻译批量翻译 + 后台配置页 |
| 翻译完成度统计 + 内容翻译概览 | ✅ 完成 | `translation-stats.service.ts` 统计 5 类实体翻译完成度 + 按语言分布 + 可视化概览页 |
| 后台仪表盘（概览数据 + 最近询盘） | ✅ 完成 | 6 项统计卡片（产品/分类/新闻/媒体/总询盘/新询盘）+ 最近 5 条询盘列表 |

### P11 — 性能优化 + 收尾

| 任务 | 状态 | 备注 |
|------|------|------|
| RTL 语言支持（布局镜像 + dir 属性） | ✅ 完成 | WebsiteShell 根据语言 isRtl 自动设置 dir 属性 |
| 图片懒加载 + next/image 优化 | ✅ 完成 | 前台所有组件已使用 next/image，配置 remotePatterns |
| 骨架屏（Skeleton） | ✅ 完成 | 6 种骨架组件（首页/产品列表/产品详情/新闻列表/新闻详情/搜索/联系），16 个 loading.tsx 文件覆盖默认+多语言路由 |
| 404 / 500 自定义页面 | ✅ 完成 | 404 页面从 UI 翻译获取文案（含降级），500 页面保留英文兜底（Client Component 限制） |
| 缓存层引入 | ✅ 完成 | `lib/data-cache.ts` 使用 `use cache` + `cacheLife('hours')` + `cacheTag`，覆盖语言/导航/设置/翻译/验证码/表单字段 |
| 返回顶部按钮 | ✅ 完成 | BackToTop 组件，滚动 400px 后显示，集成到 WebsiteShell |
| 页面切换过渡动画 | ✅ 完成 | 启用 `experimental.viewTransition` + `<ViewTransition>` 包裹页面内容，CSS 淡入/微位移动画，尊重 `prefers-reduced-motion` |
| 已归档内容 301 重定向（产品/分类/页面） | ✅ 完成 | redirects 表 + 中间件动态匹配 + 后台管理界面 |
| Lighthouse 性能 > 90 + SEO > 90 | ✅ 完成 | 动态 `<html lang>` + `theme-color` + `font-display:swap` + 分析脚本缓存 + 分页 aria-label + `revalidateTag` 适配 Next.js 16 双参数 API + 修复 TS 类型错误 |

### P12 — 数据导入导出（Phase 2）

| 任务 | 状态 | 备注 |
|------|------|------|
| 产品 CSV 导出（含多语言翻译列） | ✅ 完成 | `product-export.service.ts` + `exportProductsCsvAction` |
| 产品 CSV 导入（模板下载 + 上传 + 预览验证 + 冲突处理） | ✅ 完成 | `product-import.service.ts` + 预览/执行两步 Action + 后台 UI |
| 询盘 CSV 导出 | ✅ 完成 | `inquiry-export.service.ts` + `exportInquiriesCsvAction` |
| 后台导入导出管理页面 | ✅ 完成 | `/admin/settings/data`：导出产品/询盘 + 导入产品（预览+冲突策略） |

---

## 已知问题 / 技术债

| 问题 | 严重性 | 备注 |
|------|--------|------|
| Next.js 16 警告 middleware 约定已弃用，建议使用 proxy | 低 | 功能正常，后续迁移到 proxy 约定 |
| ~~Middleware 引入 DB 导致 Edge Runtime 编译失败~~ | ~~高~~ | ✅ 已通过拆分 `auth.config.ts`（Edge 安全）+ `auth.ts`（含 DB）解决 |
| ~~Client Component 动态导入 Service 拉入 server-only 模块~~ | ~~高~~ | ✅ `news-management.tsx` 改用 `getNewsByIdAction` Server Action 替代 |
| ~~后台页面预渲染时查询数据库失败~~ | ~~高~~ | ✅ 后台 layout 添加 `dynamic = 'force-dynamic'` |
| ~~`next-themes` 包已无引用但仍在 dependencies 中~~ | ~~低~~ | ✅ 已清理（2026-03-08） |
| 前端路由双份维护（`app/products/` + `app/[locale]/products/`） | 中 | 默认语言无前缀需要平行页面文件，后续考虑通过中间件 rewrite 统一 |
| ~~前端页面缺少 `generateMetadata`（SEO 元数据）~~ | ~~高~~ | ✅ P8 已补全所有前台页面 SEO 元数据（metadata + hreflang + JSON-LD + sitemap + robots.txt） |
| ~~前台高频数据缺少缓存（导航树、语言列表、站点设置）~~ | ~~中~~ | ✅ 已通过 `use cache` + `cacheLife` + `cacheTag` 引入缓存层，后台写操作自动 `revalidateTag` 失效 |
| 无任何测试覆盖 | 中 | 建议为 Service 层核心业务逻辑补充单元测试 |
| ~~缺少 Dockerfile，无法容器化部署~~ | ~~高~~ | ✅ 已创建 Dockerfile（standalone 多阶段构建）+ docker-compose app 服务 |
| ~~文件存储仅支持 local，容器化后丢失~~ | ~~高~~ | ✅ 已实现 S3StorageAdapter（兼容 AWS S3 / Cloudflare R2 / MinIO） |
| ~~SMTP 密码明文存储~~ | ~~高~~ | ✅ 已实现 AES-256-GCM 加密存储，通过 SMTP_ENCRYPTION_KEY 环境变量控制 |
| ~~缺少 /about 和 /page/[slug] 前台路由~~ | ~~中~~ | ✅ 已实现 about + 自定义页面路由（含多语言版本） |
| ~~Header 使用图标代替实际 Logo~~ | ~~中~~ | ✅ 已接入站点设置动态 Logo，无 Logo 时回退为图标 |
| ~~Footer 仅有导航链接，缺少企业信息/社媒/版权~~ | ~~中~~ | ✅ 已补全三栏布局 + 社媒图标 + 版权栏 + WhatsApp 浮动按钮 |

---

## 重要决策记录

| 日期 | 决策 | 原因 |
|------|------|------|
| 2026-03 | 项目命名为 Vela | 拉丁语"帆"，契合全球贸易定位，简短好记 |
| 2026-03 | 采用单 Next.js 应用架构（不做前后端分离） | AI 开发效率优先，类型端到端贯通，Server Actions 替代 REST API |
| 2026-03 | 区块系统采用注册制，不预设固定数量 | 灵活扩展，按业务需要按需开发 |
| 2026-03 | 数据库选用 PostgreSQL + Drizzle ORM | JSON 字段支持区块 config，Drizzle 类型推导强，AI 友好 |
| 2026-03 | Docker 镜像改用 postgres:17-alpine | 本地已缓存该镜像，避免拉取超时 |
| 2026-03 | 后台 UI 固定使用中文 + 暗色科技感主题 | 后台面向内部使用，无需多语言；`.admin-dark` CSS 作用域 |
| 2026-03 | 后台暗色主题改为挂载到 `<html>` 而非内部 `<div>` | 解决 Radix Portal 弹出内容（Dialog/DropdownMenu/Toast 等）无法继承暗色 CSS 变量的问题；通过 `useAdminTheme()` hook 管理 |
| 2026-03 | Sonner Toast 移除 `next-themes` 依赖 | 改用 `useDetectTheme()` 检测 `<html>` 上的 `dark` 类，避免依赖缺失的 ThemeProvider |
| 2026-03 | db 脚本统一使用 dotenv-cli 加载 .env.local | drizzle-kit 不自动读取 Next.js 的 .env.local |
| 2026-03 | 后台导航按功能域分 5 组：内容/营销/外观/多语言/系统 | 清晰的信息架构，便于后续扩展 |
| 2026-03 | 询盘管理拆分为独立页面（列表/表单配置/统计） | 功能独立，侧边栏子菜单导航 |
| 2026-03 | 设置页面拆分为独立子页面 | 站点设置/邮件配置/脚本注入各自独立 |
| 2026-03 | 用户角色简化为仅 Admin | 当前阶段不需要多角色，简化开发 |
| 2026-03 | 开发端口改为 3100 | 避免与常用端口冲突 |
| 2026-03 | 语言代码采用 BCP 47 风格作为内部统一标准，并维护 Azure/Google 映射 | 避免翻译引擎代码差异导致歧义；支持标准中文语言名称展示（如“克罗地亚语”） |
| 2026-03 | 文件存储抽象为 StorageAdapter 并默认本地实现 | 为后续切换 S3/R2 保持接口稳定，上传入口统一通过 media service |
| 2026-03 | 建立 API 与 Action 双文档机制，并要求接口变更强制同步文档 | 降低前后端联调成本，避免接口契约漂移 |
| 2026-03 | API/Action 文档新增模板段与 Deprecated 规范 | 统一文档写法，保障接口演进可追踪、可迁移 |
| 2026-03 | 媒体库模块采用“上传 API + 删除 Action”混合模式 | 上传使用 multipart API，删除走 Server Action，兼顾文件传输和业务一致性 |
| 2026-03 | 用户管理在 P1 先实现单角色 Admin 能力 | 优先满足后台账号维护与安全控制，避免过早引入复杂 RBAC |
| 2026-03 | 富文本编辑器基于 Tiptap 并采用白名单 HTML 清理 | 满足翻译友好的基础格式能力，减少粘贴外部内容带来的脏标签风险 |
| 2026-03 | 管理后台登录改为“用户名或邮箱”双模式 | 降低登录门槛，同时满足后台账号体系对用户名登录的需求 |
| 2026-03 | 登录流程增加 DB 可用性预检查 | 避免数据库连接异常时误提示“账号或密码错误”，改为明确故障提示 |
| 2026-03 | 分类拖拽采用“同级重排 + 跨级挂载”语义 | 用户将分类拖到目标分类上时自动挂为其子分类，同时保留同级重排，兼顾可用性与实现复杂度 |
| 2026-03 | 标签管理采用“slug + 翻译表”最小模型 | 满足筛选和展示的多语言需求，同时保持后续与产品标签关联模型稳定 |
| 2026-03 | 产品管理优先交付“核心生命周期 + 基础关联” | 先覆盖草稿/发布/归档与分类标签关联，保证可运营闭环；图集与参数分组在后续任务补齐 |
| 2026-03 | 产品参数采用“产品内分组 + 分组内参数”模型 | 支持多语言参数名/值、分组拖拽排序和参数跨组移动，满足产品详情页参数表配置需求 |
| 2026-03 | 产品媒体录入并入产品管理表单 | 在产品管理中直接维护主图、图集与视频链接，降低运营切换成本并贴合外贸展示习惯 |
| 2026-03 | 产品管理补充外贸商业字段 | 新增 MOQ/交期/贸易条款/付款方式/包装说明/定制支持，提升采购决策信息完整度 |
| 2026-03 | 产品编辑支持直接上传媒体与附件 | 在产品编辑表单内可直接上传主图/图集/附件并立即绑定，减少“先去媒体库再回来”的操作割裂 |
| 2026-03 | 产品编辑改为分区式交互（基础/媒体/商业/多语言） | 降低单页信息负担，默认语言优先编辑，提升后台操作顺手度与填写效率 |
| 2026-03 | 产品列表新增完成度提示与未保存拦截 | 列表可快速识别“可发布”状态，编辑弹窗关闭前拦截未保存修改，降低误操作风险 |
| 2026-03 | 页面管理先交付“页面基础信息 + 多语言 SEO”最小闭环 | 优先让运营可维护 slug/状态/首页标记/模板与标题 SEO，为后续区块编辑器接入打基础 |
| 2026-03 | 区块系统采用“先 MVP 后扩展”交付策略 | 先打通页面构建闭环（区块 CRUD/排序/前台渲染），优先支持 Hero/富文本/CTA，子项区块与更多区块类型按优先级迭代 |
| 2026-03 | Product Showcase 区块支持按分类/标签筛选 | 通过区块 config（`category_slug`、`tag_slug`、`limit`）控制展示范围，兼顾运营灵活性与实现复杂度 |
| 2026-03 | 导航管理优先交付“4 链接类型 + 多语言名称 + 树形父子” | 先满足后台可配置导航数据源，为前台 Header/Footer 动态菜单渲染打基础 |
| 2026-03 | 多语言路由采用“Middleware + 语言配置 API”模式 | Middleware 在边缘环境无法直接查库，改为调用 `/api/i18n/locales` 并做短时缓存，兼顾动态语言配置与运行时可用性 |
| 2026-03 | 前台布局采用“先动态骨架后功能补全”策略 | 先接入 Header/Footer 的动态导航与语言切换，优先验证信息架构与多语言链路，再补搜索、询盘、站点信息等营销元素 |
| 2026-03 | Cookie 弹窗文案改为严格依赖 UI 翻译键 | 前台不再硬编码提示语，统一从 `ui_translations` 读取 `cookie.title/description/accept/reject`，保证多语言一致性 |
| 2026-03 | 产品前台页面采用“先主链路、后高级筛选”迭代节奏 | 先打通总览/分类/详情与多语言路径，再补分类树、标签筛选、排序、参数表和关联推荐 |
| 2026-03-08 | 产品列表分页改为 SQL 层 LIMIT/OFFSET + COUNT | 原实现全表加载后 JS 切片，产品规模增长后性能不可接受；改为数据库层分页 |
| 2026-03-08 | 产品创建/更新包裹 `db.transaction()` | 涉及主体+翻译+分类+标签+图集+附件多表写入，需事务保证原子性 |
| 2026-03-08 | sortOrder 计算改为 `MAX(sort_order) + 1` | 原实现 `SELECT * FROM products` 取 `.length` 有全表扫描开销 |
| 2026-03-08 | 面包屑文案改为从 `ui_translations` 读取 | 修复产品页面面包屑硬编码符号，统一使用 `nav.home`/`nav.products` 翻译键 |
| 2026-03-08 | 移除 `next-themes` 依赖 | 后台主题通过 `useAdminTheme` 管理，Sonner 使用 `useDetectTheme()`，无代码依赖 |
| 2026-03-08 | 产品列表页采用"侧边栏分类树 + 工具栏筛选排序"布局 | 桌面端左侧 240px 分类树（带产品计数 + 折叠展开 + 高亮当前），移动端分类树上置；标签筛选 pill + 排序下拉在主内容区顶部 |
| 2026-03-08 | 排序 `name_asc`/`name_desc` 基于 slug 而非翻译名 | 避免排序依赖 locale join 带来的查询复杂度；slug 统一英文，排序结果跨语言一致 |
| 2026-03-08 | 产品前台新增 UI 翻译键 `product.*` | `allProducts`、`sortNewest`、`sortPopular`、`sortNameAsc`、`sortNameDesc`、`noProducts`、`totalCount`、`categories` |
| 2026-03-08 | 搜索采用"SKU ILIKE + 产品名称所有语言 ILIKE"策略 | 先通过翻译表 + 产品表两轮查询收集命中 ID，再分页加载详情，简单高效，后续可引入 pg_trgm 或全文索引 |
| 2026-03-08 | 联系我们页面先交付"UI 框架 + site_settings 读取"骨架 | 联系表单提交待 P6 询盘系统接入，Google Maps 嵌入与完整企业信息待 P10 站点设置 |
| 2026-03-08 | 新增 `settings-public.service.ts` 公共设置查询 | 前台页面读取 site_settings 联系信息的最小 Service，与后台完整设置管理（P10）解耦 |
| 2026-03-08 | 询盘篮采用 `useSyncExternalStore` + LocalStorage | 避免 React Context 重渲染，跨标签页不同步（可接受），数据持久化到 localStorage |
| 2026-03-08 | 询盘号格式 `INQ-YYYYMMDD-XXXXXX` | 日期前缀便于快速识别时间段，6 位随机码足够低碰撞概率，人类可读 |
| 2026-03-08 | Header 搜索 + 询盘篮拆为客户端子组件 `HeaderActions` | Header 保持 Server Component，仅搜索/询盘交互部分为 Client Component，最小化客户端 JS |
| 2026-03-08 | 产品卡片新增"加入询盘"按钮 | 通过 `AddToBasketButton` 客户端组件实现，卡片主体仍为服务端渲染 |
| 2026-03-08 | 询盘后台侧边栏导航启用（移除 disabled） | P6 核心功能已交付，询盘管理页面可用 |
| 2026-03-08 | 新闻系统采用与产品管理一致的四层架构模式 | Schema → Service → Action → UI，保持代码模式一致性 |
| 2026-03-08 | 新闻后台编辑采用 Tabs 分区（基础信息 / 多语言内容） | 降低单页信息负担，每种语言独立编辑含标题/摘要/富文本/SEO |
| 2026-03-08 | 新闻详情页使用 `prose` 排版样式渲染富文本 | Tailwind Typography 插件提供良好的正文阅读体验 |
| 2026-03-08 | 新增 shadcn/ui Tabs 组件（@radix-ui/react-tabs） | 新闻后台编辑器需要分区交互 |
| 2026-03-08 | SEO metadata 抽取为 `lib/seo.ts` + `buildSeoMetadata` 通用函数 | 统一 14 个前台页面的 title/description/canonical/OG/hreflang 生成逻辑，避免重复代码 |
| 2026-03-08 | JSON-LD 组件化（`components/website/seo/json-ld.tsx`） | 提供 Organization/WebSite/Product/Article/Breadcrumb 等 JSON-LD 组件，按页面类型组合 |
| 2026-03-08 | Sitemap 动态生成含多语言 alternate | 每个 URL 条目包含所有活跃语言的 alternate 链接 + x-default，符合 Google 多语言 SEO 最佳实践 |
| 2026-03-08 | 搜索页设置 `noindex` | 搜索结果页不应被搜索引擎索引，避免稀释 SEO 价值 |
| 2026-03-08 | `settings-public.service.ts` 增强为提供完整 SEO 站点信息 | 新增 `getPublicSiteInfo` 函数，一次查询返回站点名称/描述/Logo/OG 图片/社交链接等 |
| 2026-03-08 | 主题配置类型化定义（`types/theme.ts`） | 定义 ThemeConfig 接口覆盖颜色/字体/按钮/导航/布局五大维度，含默认预设值 |
| 2026-03-08 | 主题 CSS 变量通过 Server Component 注入 | `ThemeStyle` 组件读取激活主题在 SSR 时直接输出 `<style>` 到 `:root`，零客户端 JS |
| 2026-03-08 | UI 翻译管理采用"分类侧栏 + 行内编辑"交互模式 | 左侧显示分类列表含完成度，右侧翻译键列表支持行内展开编辑所有语言，缺失语言红色高亮 |
| 2026-03-08 | UI 翻译 Service 独立为 admin 版本 | `ui-translation-admin.service.ts` 提供管理功能，原 `ui-translation.service.ts` 专注前台读取 |
| 2026-03-08 | 系统设置采用 Tabs 五区分页（品牌/联系/社交/公司/多语言） | 降低单页信息负担，多语言字段内嵌二级 Tabs 按语言切换 |
| 2026-03-08 | 系统设置 Service 拆分为 admin 和 public 两个版本 | `settings-admin.service.ts` 管理完整读写，`settings-public.service.ts` 仅前台安全读取 |
| 2026-03-08 | 第三方脚本通过 `next/script` + `afterInteractive` 注入 | GA/GTM/Pixel 使用 Next.js Script 组件按最佳实践注入，自定义脚本通过 `dangerouslySetInnerHTML` |
| 2026-03-08 | 仪表盘替换为数据驱动的统计概览 | 原占位快捷入口卡片改为实时统计数据（产品/分类/新闻/媒体/询盘）+ 最近 5 条询盘 |
| 2026-03-08 | 404/500 自定义页面采用简洁居中设计 | `not-found.tsx` 提供 Back to Home + Browse Products 链接，`error.tsx` 提供 Try Again + Back to Home |
| 2026-03-08 | 返回顶部按钮在滚动 400px 后显示 | BackToTop 使用 passive scroll listener，固定右下角，smooth scroll 回顶部 |
| 2026-03-08 | RTL 支持通过 `dir` 属性在 WebsiteShell 层实现 | 根据语言 `isRtl` 标志动态设置 `dir="rtl"`，Tailwind CSS `rtl:` 变体可直接使用 |
| 2026-03-08 | 301 重定向采用"DB 存储 + 中间件缓存匹配"方案 | `redirects` 表存储规则，中间件通过 API 加载并 60s 缓存，后台可视化管理 |
| 2026-03-08 | next/image 统一替换前台所有 `<img>` | 配置 `remotePatterns` 允许本地域名，所有图片自动获得懒加载 + WebP/AVIF 转码 + 多尺寸优化 |
| 2026-03-08 | Footer 采用三栏响应式布局 + WhatsApp 浮动按钮 | 品牌栏（Logo/公司/社媒）+ 快速链接 + 联系方式，底部版权栏；WhatsApp 固定右下角悬浮 |
| 2026-03-08 | Header Logo 从站点设置动态读取 | 有 Logo 时展示 `next/image`（priority 预加载），无 Logo 回退为 Home 图标 |
| 2026-03-08 | `PublicSiteInfo` 扩展为包含所有前台公共字段 | 新增 whatsapp/contactFax/logoDarkUrl/footerText/copyright/contactCta/socialPinterest/socialAlibaba |
| 2026-03-08 | 邮件服务基于 Nodemailer + DB 配置动态创建 transporter | 每次发送从 `site_settings` 读取 SMTP 配置，port 465 自动 secure；测试邮件后台可用 |
| 2026-03-08 | 询盘邮件通知采用异步非阻塞模式 | 询盘创建后立即返回成功，后台通过 `Promise.allSettled` 异步发送双向邮件，失败不影响用户体验 |
| 2026-03-08 | hCaptcha 采用 invisible 模式 + 服务端验证 | 用户无感知，提交时自动触发验证；未配置 key 时自动跳过（graceful degradation） |
| 2026-03-08 | 验证码配置合并到脚本注入设置页 | hCaptcha Site Key + Secret Key 与 GA/GTM/翻译 API Key 统一管理 |
| 2026-03-08 | 询盘表单自定义字段采用四层架构 | Schema → Service → Action → UI，支持 5 种字段类型、拖拽排序、多语言标签 |
| 2026-03-08 | Azure Translator 服务封装批量翻译（50 条/批） | API 限制每批 50 条文本，自动分批处理；语言代码通过 `azure_code` 映射表转换 |
| 2026-03-08 | 翻译完成度统计覆盖 5 类实体 | 产品/分类/页面/新闻/UI 翻译，按语言维度统计已翻译/总量，可视化进度条 |
| 2026-03-08 | 产品详情页拆分为 Gallery + Actions 独立客户端组件 | 主组件保持 Server Component，仅图片交互和询盘按钮为 Client Component，最小化 JS |
| 2026-03-08 | 缓存层采用 Next.js 16 `use cache` + `cacheLife` + `cacheTag` | `lib/data-cache.ts` 统一缓存前台高频数据，后台写操作自动 `revalidateTag` 失效 |
| 2026-03-08 | 区块系统扩展至 9 种类型 | 新增 feature_grid/carousel_banner/stats/faq/two_column，复用现有 items + config 数据模型 |
| 2026-03-08 | 联系表单接入询盘系统（products=[]） | 联系页面提交也作为询盘记录，无产品但保留客户信息和留言 |
| 2026-03-08 | 404 页面改为异步 Server Component 获取翻译 | try/catch 包裹防止 DB 不可用时崩溃，英文兜底 |
| 2026-03-08 | 骨架屏采用 shadcn/ui Skeleton + 共享组件架构 | 6 种骨架组件在 `skeletons/` 目录下复用，loading.tsx 按路由段分配 |
| 2026-03-08 | 页面过渡采用 View Transitions API | Next.js 16 `experimental.viewTransition` + React `<ViewTransition>` 组件，CSS 淡入动画 |
| 2026-03-08 | `<html lang>` 改为动态读取 cookie | 从中间件设置的 `vela_locale` cookie 读取，解决 Lighthouse SEO 审计中 `html[lang]` 不匹配的问题 |
| 2026-03-08 | `revalidateTag` 适配 Next.js 16 双参数 API | 所有 `revalidateTag(tag)` 改为 `revalidateTag(tag, 'max')`，使用 stale-while-revalidate 语义 |
| 2026-03-08 | Dockerfile 采用 standalone 多阶段构建 | 最终镜像仅含 `server.js` + `.next/static` + `public`，镜像体积最小化 |
| 2026-03-08 | S3StorageAdapter 兼容 S3/R2/MinIO | 通过 `@aws-sdk/client-s3` 实现，`forcePathStyle: true` 兼容非 AWS 端点 |
| 2026-03-08 | SMTP 密码采用 AES-256-GCM 加密存储 | `SMTP_ENCRYPTION_KEY` 环境变量控制，未配置时向后兼容明文 |
| 2026-03-08 | `/about` 作为固定路径渲染 slug=about 的页面 | 与 `/page/[slug]` 通用页面系统共用 `getPublishedPageBySlug` + 区块渲染 |
| 2026-03-08 | 安全加固：限流 + 安全响应头 | 登录（15min/10次）、询盘（10min/5次）、上传（1min/20次）限流 + X-Content-Type-Options/X-Frame-Options 等头 |
| 2026-03-08 | 产品导入采用"预览+确认"两步流程 | 先解析校验展示错误和有效行数，用户确认后才执行写库，支持"跳过/覆盖"冲突策略 |
| 2026-03-08 | CSV 导出带 BOM 头 | 确保 Excel 正确识别 UTF-8 编码，避免中文乱码 |
| 2026-03-08 | 区块系统扩展至 18 种类型 | 新增 9 种 B2B 核心区块，覆盖信任层（Logo+评价）、产品导航、多媒体、互动表单 |
| 2026-03-08 | Hero 区块增加背景大图模式 | 通过第一个子项图片自动切换为全屏背景 + 暗色遮罩 + 白色文字模式 |
| 2026-03-08 | category_nav 作为数据驱动区块 | 类似 product_showcase，从 DB 读取分类（含图片+产品数），section.service.ts 统一查询 |
| 2026-03-08 | 后台区块类型选择器分组展示 | 18 种区块按布局/内容/媒体/交互/其他五组 optgroup 展示，带中文说明 |
